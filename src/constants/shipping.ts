/**
 * Shipping cost for SIM delivery (when user selects "I need a SIM")
 * Applied to prepaid, combo, and contract dynamic plan purchases
 */
export const SHIPPING_COST_RANDS = 65
export const SHIPPING_COST_CENTS = 6500  // R65 in cents
