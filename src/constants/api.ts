/**
 * API timeout values in milliseconds.
 */
export const API_TIMEOUT_MS = 120_000 // 2 minutes — for heavy backend operations
export const API_TIMEOUT_SHORT_MS = 60_000 // 1 minute — for lighter operations
